{
  "name": "blackbox-enterprises",
  "version": "1.0.0",
  "private": true,
  "description": "Blackbox Enterprises — Enterprise automation landing page with Stripe checkout",
  "scripts": {
    "test:e2e": "npx playwright test",
    "test:e2e:headed": "npx playwright test --headed",
    "test:e2e:ui": "npx playwright test --ui",
    "stripe:portal": "echo 'Open https://dashboard.stripe.com/test/products to manage products'",
    "serve": "npx serve . -l 3333"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "serve": "^14.2.0"
  },
  "dependencies": {
    "@stripe/stripe-js": "^5.5.0"
  }
}
